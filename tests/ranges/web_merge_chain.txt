# Edge case: transitive web merging.
# Three ranges of variable v that pairwise overlap and must all merge
# into a SINGLE web via the fixed-point loop in WebBuilder::build.
#   v(1..5) overlaps v(5..9) at 5; v(5..9) overlaps v(9..12) at 9.
# Expected: 1 web for v, plus an independent w that does not merge.
v: 1+,2,3,4,5-
v: 5+,6,7,8,9-
v: 9+,10,11,12-
w: 2+,3,4-
